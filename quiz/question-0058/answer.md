A boolean expression is a Java expression that, when evaluated, returns a `boolean` value: `true` or `false`. And a boolean expression can be short-ciruit.

The output of the program is: 

```
return true
return false
```