There are several ways. For example, you can use format string with `%n`. For example

```java
String.format("%s%n", text);
```

Or you can use `System.lineSeparator()` method. Or you can use `System.getProperty("line.separator")`