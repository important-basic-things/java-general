What is the executing result of the following program?

```java
// suppose calculateAddress() returns non-zero.
int address = calculateAddress();
if (!address) {
  System.out.println("Invalid address");
} else {
  System.out.println("Valid address");
}
```